import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "WeatherApp - Beautiful Weather Forecasts",
  description: "Get beautiful, accurate weather forecasts for any location in the world. Powered by Open-Meteo.",
  keywords: ["weather", "forecast", "temperature", "rain", "snow", "humidity"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
